# Copy to .env (git-ignored) and set a password before running `docker compose up`.
# SQL Server requires at least 8 characters from 3 of 4 groups: uppercase, lowercase, digits, symbols.
# Avoid ';' (breaks the connection string) and '$' (interpolated by docker compose).
MSSQL_SA_PASSWORD=
